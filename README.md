# astro-loader
Python-based data ingestion subsystem that collects, validates, cleans, and loads raw data into PostgreSQL staging tables. Designed with modularity and reliability in mind, it handles data quality checks, deduplication, and error tracking while separating valid records from rejected ones for auditing and review.
